{
  "name": "sms",
  "version": "1.0.0",
  "private": true,
  "description": "School Management System for private and public schools in Tanzania",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web",
    "build": "npm run build --workspace=server && npm run build --workspace=web",
    "test": "npm run test --workspace=server",
    "db:migrate": "npm run db:migrate --workspace=server",
    "db:seed": "npm run db:seed --workspace=server",
    "bootstrap": "npm run bootstrap --workspace=server",
    "bootstrap:super-admin": "npm run bootstrap:super-admin --workspace=server"
  },
  "engines": {
    "node": ">=20"
  }
}
